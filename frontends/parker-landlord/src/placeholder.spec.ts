export {} // Just here to make it a module, not needed if there are imports

describe('placeholder', () => {
  it('is just a placeholder until I write real tests', () => {
    expect(10).toBe(10)
  })
})
