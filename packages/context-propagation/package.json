{
  "name": "@parker/context-propagation",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf node_modules/ dist/ coverage/ .turbo/",
    "format": "eslint . --ext .ts --fix",
    "lint": "tsc --noEmit && eslint . --ext .ts",
    "test": "LOG_LEVEL=\"${LOG_LEVEL:-off}\" jest src/",
    "test:watch": "LOG_LEVEL=\"${LOG_LEVEL:-off}\" jest --watch src/"
  },
  "dependencies": {
    "tslib": "^2.7.0"
  },
  "devDependencies": {
    "@parker/tsconfig": "*",
    "@types/jest": "^29.5.12",
    "@types/node": "^22.5.0",
    "eslint": "^8.57.0",
    "eslint-config-custom": "*",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.4"
  }
}
